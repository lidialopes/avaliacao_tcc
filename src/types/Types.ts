export interface Tcc {
	id: string;
	tema: string;
}

export interface Aluno {
	id: string;
	nome: string;
	matricula: string;
	curso: string;
}

export interface Professor {
	id: string;
	nome: string;
}